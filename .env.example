# ============================================
# GitHub User Analytics - Environment Configuration
# ============================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your GitHub token below
# 3. (Optional) Add Vercel KV credentials for caching
#
# SECURITY:
# - GITHUB_TOKEN is server-side only (api/github-proxy.ts)
# - NEVER use VITE_GITHUB_TOKEN in production!
# - .env.local is gitignored automatically
#
# ============================================

# --------------------------------------------
# GitHub Personal Access Token (REQUIRED)
# --------------------------------------------
# Create token at: https://github.com/settings/tokens/new
# Token name: GitHub User Analytics
# Scopes required:
#   ✅ read:user      - Read user profile data
#   ✅ user:email     - Read email addresses (optional)
#
# ⚠️ IMPORTANT:
# - Use GITHUB_TOKEN for backend (secure, production-ready)
# - Use VITE_GITHUB_TOKEN only for development (exposed in bundle)
#
GITHUB_TOKEN=ghp_your_token_here

# For development without backend (NOT RECOMMENDED):
# VITE_GITHUB_TOKEN=ghp_your_token_here

# --------------------------------------------
# Vercel KV Cache (OPTIONAL, for production)
# --------------------------------------------
# Get credentials from: Vercel Dashboard → Storage → KV → .env.local
# Enables 30-minute caching of GitHub API responses
# Reduces API rate limit usage
#
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# --------------------------------------------
# GitHub OAuth (OPTIONAL, Phase 7)
# --------------------------------------------
# For per-user authentication and personal rate limits
# Create OAuth App at: https://github.com/settings/developers
#
# Application settings:
#   - Homepage URL: https://your-app.vercel.app
#   - Callback URL: https://your-app.vercel.app/api/auth/callback
#
# ⚠️ SECURITY:
# - NEVER expose Client Secret in client bundle
# - These are server-side only
#
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=

# --------------------------------------------
# Feature Flags (OPTIONAL)
# --------------------------------------------
# Uncomment to enable experimental features:
# VITE_ENABLE_NEW_METRICS=true
# VITE_ENABLE_TIMELINE=true
